int main() {
    tuple<string, bool, int> z = ["a", true, 12];
    
    tuple<int> t = [12];
    bool b;
    string s;
    int i;
    { int s = 10; <s, b, i> = z; }
    <s, b, i> = ["inne", false, 13];

    // Zagnieżdzenie
    <s, <b, i>> = ["zagnieżdznie", [false, 14]];

    // Zagnieżdzenie2;
    <s, t> = ["zagnieżdżenie2", [15]];
}
