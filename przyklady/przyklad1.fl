int g() {
    return -12;
}

void h(function< int (int ref)> g, int h) {
    int z, w = 0 * 12;
    printInt(h + 10);
    return;
}

int main() {
    int f() {
        return f();
    };

    int g(int ref x) {
        { int x; }
        int y = g(x);
        void g() {
            return;
        };
    };
    h(g);
    string z = "g";
    int x = 0;
    { string x; }
}

