void main() {
    
    int isEven(int x) {
        return (x == 2);
    };


    // Funkcja wbudowana assert kończy program w zależności od boola
    assert(isEven(4)); 
    

    // Zmienne read-only
    const int x = 3;
    // x = 4; <- zwróci błąd.


    // Konkatenacja stringów.
    print("hello" + " world!");
}