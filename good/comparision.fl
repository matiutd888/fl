int main() {
    assert("" < "a");
    assert([1, ["a"]] < [2, ["b"]]);
    assert(!([1, ["b"]] < [2, ["a"]]));
    assert(!([1, ["b"]] == [2, ["a"]]));
    assert(!([1, ["b"]] > [2, ["a"]]));
    return 0;
}
