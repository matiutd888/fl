int silnia(int n) {
    if (n <= 1) {
        return 1;
    }
    return n * silnia(n - 1);
}

void g(tuple<int> ref t) {
    t = [10];
}

int main() {
    tuple<int> t = [14];
    g(t);
    int s;
    <s> = t;
    printInt(s);
    int f(int ref x) {
        x = x + 1;
        return 0;
    };
    
    int x = 3;
    f(x);
    {
        printInt(x);
        string x = "siema";
        printString(x);
    } 
    // printInt(silnia(30));
    return 0;
}
